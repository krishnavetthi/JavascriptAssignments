//08. Demonstrate the use of trim() to remove any trailing whitespace at the beginning and the end of a string.

let stringVariable = "     This is a string         ";
console.log("Using trim() method to remove any trailing whitespace at the beginning and the end of a string.");
console.log(`This is the string -> ${stringVariable}: the whitespaces are removed now ->${stringVariable.trim()}!`);